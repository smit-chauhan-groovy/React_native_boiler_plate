import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    flex: 1,
    height: 200,
    borderRadius: 5,
  },
  imageView: {
    width: '100%',
    height: 170,
    borderRadius: 5,
  },
});

export default styles;
